{
  "default": {
    "requireModule": ["ts-node/register"],
    "require": [
      "src/core/CustomWorld.ts",
      "src/hooks/*.ts",
      "src/steps/*.ts"
    ],
    "format": ["progress", "summary", "./reporter.cjs"]
  }
}
